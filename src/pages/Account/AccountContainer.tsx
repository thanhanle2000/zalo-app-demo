import React, { memo } from "react";
import { Page } from "zmp-ui";

const AccountContainer = () => {
  return <Page className="page">AccountContainer</Page>;
};
export default memo(AccountContainer);
